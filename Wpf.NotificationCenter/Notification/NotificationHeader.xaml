<UserControl x:Class="Wpf.NotificationCenter.NotificationHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf.NotificationCenter"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal" ToolTip="{Binding NotificationType}">
        <materialDesign:PackIcon
            VerticalAlignment="Center"
            Kind="BellAlert" />
        <Label MaxWidth="{Binding AlertMaxWidth, RelativeSource={RelativeSource AncestorType={x:Type local:NotificationCenter}}}">
            <TextBlock FontWeight="{Binding TitleWeight, RelativeSource={RelativeSource AncestorType={x:Type local:Notification}}}"
                       TextWrapping="Wrap" Text="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type local:Notification}}}" />
        </Label>
    </StackPanel>
</UserControl>
